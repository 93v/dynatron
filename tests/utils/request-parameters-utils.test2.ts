// import { convertRawProjectionExpression } from "../../src/utils/request-parameters-utils";
// import { RequestParameters } from "../../types/request";

// describe("Projection Expressions", () => {
//   test("should return the same object if no projection expressions", () => {
//     const requestParameters: RequestParameters = {};

//     expect(convertRawProjectionExpression(requestParameters)).toMatchObject(
//       requestParameters,
//     );
//   });
// });
